import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card"

export default function KinshipPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Parentesco</CardTitle>
        <CardDescription>Gerenciamento de tipos de parentesco</CardDescription>
      </CardHeader>
      <CardContent>
        <p>Nesta página, você pode gerenciar os diferentes tipos de parentesco utilizados no sistema.</p>
      </CardContent>
    </Card>
  )
}
