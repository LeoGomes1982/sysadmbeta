import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card"

export default function SupervisorTablePage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Tabela de Supervisor</CardTitle>
        <CardDescription>Gerenciamento da tabela de Supervisor</CardDescription>
      </CardHeader>
      <CardContent>
        <p>Nesta página, você pode visualizar e editar a tabela específica para Supervisores.</p>
      </CardContent>
    </Card>
  )
}
