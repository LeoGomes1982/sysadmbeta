"use client"

import { useState } from "react"
import type { Avaliacao } from "@/types" // Assuming Avaliacao is a type defined in a separate file

export default function PerformanceEvaluationPage() {
  const [avaliacoes, setAvaliacoes] = useState<Avaliacao[]>([])

  // Importing Avaliacao type
  // Assuming the rest of the code is here and does not need modification
}
