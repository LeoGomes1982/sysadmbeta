import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card"

export default function CalendarPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Calendário</CardTitle>
        <CardDescription>Gerenciamento de calendários e eventos</CardDescription>
      </CardHeader>
      <CardContent>
        <p>Nesta página, você pode visualizar e gerenciar calendários e eventos do sistema.</p>
      </CardContent>
    </Card>
  )
}
